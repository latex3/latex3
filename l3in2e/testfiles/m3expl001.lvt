%
% Copyright (C) 2010 LaTeX3 Project
% 

\documentclass{minimal}
\input{regression-test}

% all this tests is that the package and its sub-components load properly as
% sometimes changes might introduce errors not caught at all.
%
% downside is that any changes will require to update the tlg file

% run etex outside so that its pace in the tree doesn't show up, normally this
% is called by l3names.sty

\RequirePackage{etex}

\START
\RequirePackage{expl3}
\END

